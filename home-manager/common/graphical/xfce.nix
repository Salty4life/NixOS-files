{...}: {
  xfconf.settings = {
    xfce4-keyboard-shortcuts = {
      #This file is to configure my own shortcuts into xfce in the near future, for now it's only a stub

      #"commands/custom/<Super>" = "xfce4-popup-applicationsmenu";
      # "commands/custom/<KEY_MICMUTE>" = "xfce4-mixer";
    };
  };
}
